pipeline {
    agent any
    parameters {
        string(name: 'NAME', Value: 'Thanit')
        string(name: 'Ages', Value: '21')
    }
    stages {
        stage('Example') {
            steps {
                echo "Hello from Slave ${params.NAME} of ${params.CITY}"
                
            }
        }
    }
}
