pipeline {
    agent any
    parameters {
        string(name: 'NAME', defaultValue: 'Tuchsanai')
        string(name: 'NAME', defaultValue: 'Tuchsanai')
    }
    stages {
        stage('Example') {
            steps {
                echo "Hello from Slave ${params.NAME} of ${params.CITY}"
                
            }
        }
    }
}
