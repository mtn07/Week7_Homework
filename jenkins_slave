pipeline {
    agent any
    parameters {
        string(name: 'NAME', defaultValue: 'Thanit')
        string(name: 'Ages', defaultValue: '21')
    }
    stages {
        stage('Example') {
            steps {
                echo "Hello from Slave ${params.NAME} of ${params.Ages}"
                
            }
        }
    }
}
